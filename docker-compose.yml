services:
  rendez-vous:
    build: .
    image: ${IMAGE:-rendez-vous:latest}
    restart: unless-stopped
    env_file: .env
    ports:
      - "${HOST_PORT:-3000}:3000"
    volumes:
      - rdv-data:/data

volumes:
  rdv-data:
